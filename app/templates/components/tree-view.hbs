<ul>
  {{#each data as |item|}}
    {{#if (or showAll (eq item.status "active"))}}
      {{tree-view-item 
        showAll=showAll
        data=item select=(action 'select') add=add}}
    {{/if}}
  {{/each}}
</ul>

